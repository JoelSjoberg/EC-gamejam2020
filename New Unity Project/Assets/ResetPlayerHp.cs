using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResetPlayerHp : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        GameStats.hp = 3;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
